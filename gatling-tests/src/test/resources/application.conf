simulation {
  baseUrl = "http://localhost:8765"
  baseUrl = ${?baseUrl}

  load {
    rampUsers = 5
    rampUsers = ${?rampUsers}
    
    constantUsers = 10
    constantUsers = ${?constantUsers}
    
    rampDuration = 30
    rampDuration = ${?rampDuration}
    
    testDuration = 60
    testDuration = ${?testDuration}
    
    burstUsersPerSec = 30
    burstUsersPerSec = ${?burstUsersPerSec}
  }
}
