apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: prod-secret-store
spec:
  provider:
    fake:
      data:
        - key: /prod/retailstore/config-server-credentials
          versionMap:
            CONFIG_SERVER_PWD: "prod-config-pass"
            CONFIG_SERVER_USR: "prod-usr"
            SPRING_CLOUD_CONFIG_PASSWORD: "prod-config-pass"
            SPRING_CLOUD_CONFIG_USERNAME: "prod-usr"
        - key: /prod/retailstore/keycloak-admin-credentials
          versionMap:
            KEYCLOAK_ADMIN: "admin"
            KEYCLOAK_ADMIN_PASSWORD: "prod-kc-pass"
        - key: /prod/retailstore/postgres-credentials
          versionMap:
            POSTGRES_PASSWORD: "prod-pg-pass"
            POSTGRES_USER: "appuser"
            username: "appuser"
            password: "prod-pg-pass"
        - key: /prod/retailstore/webapp-oauth2-credentials
          versionMap:
            OAUTH2_CLIENT_SECRET: "prod-oauth-secret"
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: config-server-credentials
  namespace: retailstore
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: prod-secret-store
    kind: ClusterSecretStore
  target:
    name: config-server-credentials
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: /prod/retailstore/config-server-credentials
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: keycloak-admin-credentials
  namespace: retailstore
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: prod-secret-store
    kind: ClusterSecretStore
  target:
    name: keycloak-admin-credentials
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: /prod/retailstore/keycloak-admin-credentials
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-credentials
  namespace: retailstore
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: prod-secret-store
    kind: ClusterSecretStore
  target:
    name: postgres-credentials
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: /prod/retailstore/postgres-credentials
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: webapp-oauth2-credentials
  namespace: retailstore
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: prod-secret-store
    kind: ClusterSecretStore
  target:
    name: webapp-oauth2-credentials
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: /prod/retailstore/webapp-oauth2-credentials
